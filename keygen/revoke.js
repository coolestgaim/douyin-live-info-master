/**
 * ?????? - ????? SHA256 ???????? COS ???
 * ??: node revoke.js <64???>
 */
const crypto = require('crypto');
const key = process.argv[2];
if (!key) { console.log('??: node revoke.js <64???>'); process.exit(1); }
const hash = crypto.createHash('sha256').update(key.trim()).digest('hex');
console.log('');
console.log('??: ' + key.slice(0, 16) + '...');
console.log('SHA256: ' + hash);
console.log('');
console.log('??? SHA256 ??? revoked_list.json ??????????');
